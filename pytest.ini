[pytest]
testpaths = tests

# 프로젝트 루트를 sys.path 에 넣어야 `from backend.rag import ...` 가 동작한다.
pythonpath = .

# --strict-markers: 오타 난 마커(@pytest.mark.asyncoi)를 조용히 무시하지 않고 에러로 만든다.
addopts = -q --strict-markers
